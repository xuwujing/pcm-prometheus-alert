# 开发记录

## 2026-06-15

1. 明确项目目标：Spring Boot starter 化的公共告警推送组件。
2. 参考旧项目 `bajcommon-alarm` 的异步推送、异常采集、指标告警思路。
3. 去除 Dubbo 作为主架构前提。
4. 确定第一阶段只做最小可用闭环：
   - 异常告警
   - 基础请求告警
   - JVM / 基础指标告警
   - webhook 推送
   - 降噪
5. 新增设计文档目录，作为后续多智能体开发依据。
6. 根据评审意见调整为 Spring Boot 2.x 优先，后续兼容 3.x 和 1.x。
7. 补充核心类、核心方法、核心枚举、链路流程、README、demo 与压测说明。
8. 明确本地最小实现不依赖 Prometheus、SkyWalking、ELK。

## 待办

1. 创建 Maven 父工程。
2. 明确包名与 artifactId。
3. 设计告警消息结构。
4. 定义 starter 自动装配类。
5. 定义示例工程。
